<Window x:Class="ETS_reminder.ProfileSetupWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Welcome to ETS Reminder"
        Width="420" Height="480"
        WindowStartupLocation="CenterScreen"
        ResizeMode="NoResize"
        Background="{DynamicResource WindowBackground}">

    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#E67E22"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="24,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D35400"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style TargetType="Button" x:Key="ColorButton">
            <Setter Property="Width" Value="32"/>
            <Setter Property="Height" Value="32"/>
            <Setter Property="Margin" Value="3"/>
            <Setter Property="BorderThickness" Value="2"/>
            <Setter Property="BorderBrush" Value="Transparent"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border x:Name="Bd"
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="16"/>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <ScrollViewer VerticalScrollBarVisibility="Auto">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <StackPanel Grid.Row="0" HorizontalAlignment="Center" Margin="0,0,0,25">
            <TextBlock Text="Welcome!"
                       FontSize="26" FontWeight="Bold"
                       Foreground="#E67E22"
                       HorizontalAlignment="Center"/>
            <TextBlock Text="Set up your profile to get started"
                       FontSize="13" Foreground="{DynamicResource SubTextColor}"
                       HorizontalAlignment="Center" Margin="0,5,0,0"/>
        </StackPanel>

        <!-- Avatar Preview -->
        <Border Grid.Row="1"
                x:Name="AvatarPreview"
                Width="70" Height="70"
                CornerRadius="35"
                Background="#E67E22"
                HorizontalAlignment="Center"
                Margin="0,0,0,20">
            <TextBlock x:Name="AvatarInitials"
                       Text="?"
                       FontSize="28" FontWeight="Bold"
                       Foreground="White"
                       HorizontalAlignment="Center"
                       VerticalAlignment="Center"/>
        </Border>

        <!-- Form -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Display Name *" FontSize="13"
                       Foreground="{DynamicResource TextColor}" Margin="0,0,0,4"/>
            <TextBox x:Name="NameTextBox" FontSize="14" Padding="8,6"
                     Background="{DynamicResource TextBoxBackground}"
                     Foreground="{DynamicResource TextBoxForeground}"
                     BorderBrush="{DynamicResource TextBoxBorder}"
                     TextChanged="NameTextBox_TextChanged"/>

            <TextBlock Text="Email" FontSize="13"
                       Foreground="{DynamicResource TextColor}" Margin="0,12,0,4"/>
            <TextBox x:Name="EmailTextBox" FontSize="14" Padding="8,6"
                     Background="{DynamicResource TextBoxBackground}"
                     Foreground="{DynamicResource TextBoxForeground}"
                     BorderBrush="{DynamicResource TextBoxBorder}"/>

            <TextBlock Text="Team / Role" FontSize="13"
                       Foreground="{DynamicResource TextColor}" Margin="0,12,0,4"/>
            <TextBox x:Name="RoleTextBox" FontSize="14" Padding="8,6"
                     Background="{DynamicResource TextBoxBackground}"
                     Foreground="{DynamicResource TextBoxForeground}"
                     BorderBrush="{DynamicResource TextBoxBorder}"/>

            <TextBlock Text="Avatar Color" FontSize="13"
                       Foreground="{DynamicResource TextColor}" Margin="0,12,0,4"/>
            <WrapPanel x:Name="ColorPanel"/>
        </StackPanel>

        <!-- Button -->
        <Button Grid.Row="4"
                x:Name="SaveButton"
                Content="Get Started"
                Style="{StaticResource PrimaryButton}"
                HorizontalAlignment="Center"
                Click="SaveButton_Click"/>
    </Grid>
    </ScrollViewer>
</Window>
