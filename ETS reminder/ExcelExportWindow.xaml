<Window x:Class="ETS_reminder.ExcelExportWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Export to Excel"
        Width="650" Height="500"
        WindowStartupLocation="CenterScreen"
        ResizeMode="CanResizeWithGrip"
        Background="{DynamicResource WindowBackground}"
        PreviewKeyDown="Window_PreviewKeyDown">

    <Window.Resources>
        <Style TargetType="Button" x:Key="PrimaryButton">
            <Setter Property="Background" Value="#E67E22"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Padding" Value="20,8"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="4"
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#D35400"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="{DynamicResource DisabledButtonBackground}"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Title -->
        <TextBlock Grid.Row="0"
                   Text="Export to Excel"
                   FontSize="22" FontWeight="Bold"
                   Foreground="#E67E22"
                   Margin="0,0,0,5"/>

        <TextBlock Grid.Row="1"
                   Text="Paste the polished report from Copilot below, then click Generate Excel."
                   FontSize="13"
                   Foreground="{DynamicResource SubTextColor}"
                   Margin="0,0,0,5"/>

        <TextBlock Grid.Row="2"
                   Text="Expected format:  [Apr 01 2026 | Block 1] followed by paragraph text, one per line."
                   FontSize="11"
                   Foreground="{DynamicResource SubTextColor}"
                   Margin="0,0,0,10"/>

        <!-- Paste area -->
        <TextBox Grid.Row="3"
                 x:Name="PasteTextBox"
                 AcceptsReturn="True"
                 TextWrapping="Wrap"
                 VerticalScrollBarVisibility="Auto"
                 FontSize="13"
                 Padding="8,6"
                 SpellCheck.IsEnabled="False"
                 Background="{DynamicResource TextBoxBackground}"
                 Foreground="{DynamicResource TextBoxForeground}"
                 BorderBrush="{DynamicResource TextBoxBorder}"
                 Margin="0,0,0,10"/>

        <!-- Status -->
        <TextBlock Grid.Row="4"
                   x:Name="StatusText"
                   Text=""
                   FontSize="12"
                   Foreground="{DynamicResource SubTextColor}"
                   Margin="0,0,0,10"/>

        <!-- Buttons -->
        <StackPanel Grid.Row="5" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Generate Excel"
                    Style="{StaticResource PrimaryButton}"
                    Click="GenerateExcel_Click"
                    Margin="0,0,10,0"/>
            <Button Content="Cancel"
                    FontSize="14"
                    Padding="20,8"
                    Click="Cancel_Click"/>
        </StackPanel>
    </Grid>
</Window>
